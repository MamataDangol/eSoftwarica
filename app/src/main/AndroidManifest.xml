<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mamata.esoftwarica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adapter.Students"></activity>
        <activity android:name=".Fragments.HomeFragmentsActivity" />
        <activity android:name=".Fragments.AddFragmentsActivity" />
        <activity android:name=".Fragments.AboutFragmentActivity" />
        <activity android:name=".Adapter.Fragments.AboutFragmentActivity" />
        <activity android:name=".EditStudentActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>